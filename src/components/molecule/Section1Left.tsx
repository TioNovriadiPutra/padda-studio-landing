import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Section1Left = () => {
	return (
		<View>
			<Text>Section1Left</Text>
		</View>
	);
};

export default Section1Left;

const styles = StyleSheet.create({});
